# Server Configuration
HWSHOST=0.0.0.0
HWSPORT=8000

# SSL Configuration
# Set to true to enable HTTPS and WSS (WebSocket Secure)
# When enabled, self-signed certificates will be automatically generated if they don't exist
SSL_ENABLED=false
SSL_CERT_PATH=./certs/cert.pem
SSL_KEY_PATH=./certs/key.pem

# Storage Configuration
DISK_SPACE=100GB
VERSION=1.1.0
ROOT_DIR=./meopanel/data
LOG_DIR=./meopanel/logs
INSTANCE_DIR=./meopanel/instances